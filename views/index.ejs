<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Very vulnerable app</title>
</head>

<body>
    <div class="container">
        <div class="content">
            <h1>Search for items</h1>
            <form>
                <input type="text" name="q" id="search">
                <button type="submit">Search</button>
            </form>
            <% if (items) { %>
                <table>
                    <thead>
                        <tr>
                            <th>name</th>
                            <th>price</th>
                            <th>in stock</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% items.forEach(x => { %> 
                            <tr>
                                <td><%= x.name %></td>
                                <td><%= x.price %></td>
                                <td><%= x.instock %></td>
                            </tr>
                        <% }) %> 
                    </tbody>
                </table>
                <% } %>
        </div>
    </div>
</body>

</html>